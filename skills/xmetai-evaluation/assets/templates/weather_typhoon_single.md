# 确定性单成员台风报告骨架

覆盖 `weather_typhoon_single_<模型>` 跑出来的逐起报归档：每个场次一份
`tc<编号>_<起报时刻>.csv` + `_meta.json`，外加一份拼起来的 `typhoon.csv`。
同一 config 家族还有一份：[`weather_typhoon_ens.md`](./weather_typhoon_ens.md)（集合口径，
方案 A/B 两种汇总）——章节写法一致，差别在误差表是"集合平均位置的误差"还是"成员误差的平均"，
以及强度项有没有。

**照这个填空就是最终报告。** `{…}` 是占位符，**其余每一个字逐字照抄，不许改**——
包括标点、加粗、表头与分隔行、图注的位置。

这份骨架的章节结构来自被模板化的那份原报告
（`AIFS、伏羲、FGVP 与风清台风预报误差对比报告.md`），走 `# 1.`…`# 7.`，
每张表由它上方的粗体小标题引导（**表不编号**），只有两张图带 `图 N：` 图注。

**与 `weather_rmse_single.md` 的关键差别**：台风报告里**没有相对化指标**。
路径误差就是 km、风速误差就是 m/s、气压误差就是 hPa，不做几何均值归一化，
所以没有 RMSE 家族那套"相对 RMSE / 平均排名分 / 综合对比"。模型之间比的是绝对量。

**编号契约**：`图 N：` 必须是 `1..2` 连续；每张图必须在正文里出现过。

> **本骨架目前没有测试守着**（`tests/` 在这个仓库里是空的）。
> **占位符不要跨行写**——`{` 和 `}` 两边都算占位符标记。
>
> **每个时效必须带样本数。** 这不是可选项：台风的有效样本随起报个例的
> 生命史长短变化，越到后期时效参与平均的个例越少（同一个台风只有
> 生命力足够长的那些起报时刻能配上）。不写样本数的长时效均值等于没有分母。

---

<!-- skeleton: A -->
````markdown
# XMETAI {模型数}模型台风路径与强度预报误差检验报告（单成员）

**归档：{归档名}**

报告日期：{报告日期}    评估层级：输出级复核

## 报告定位与衔接

报告类型：台风路径/强度检验（typhoon/det）。预测目标是**诊断出来的台风中心位置**与**中心强度**，不是格点场，因此不适用 RMSE / ACC / 活跃度那一套场检验指标。{衔接说明}

路径误差直接使用逐场次 CSV 的 `track_err_km`（预报中心与 babj 实况中心的大圆距离，km），**不做相对化**，模型之间比绝对量。中心由预报场 msl 链式诊断：从起报时刻的实况位置出发，前 {初期小时}h 用 ±{初期搜索半径}° 方框、其后用 ±{搜索半径}° 方框，逐时效取框内最低气压点作为该时效的中心，并以前一时效的中心作为下一时效的搜索中心；强度取**当前诊断中心** ±{强度半径}° 方框内的 10 m 风速最大值。

本报告的实况来源是 babj diamond7 报文，时间为**北京时**（UTC+8）；预报起报时刻为 UTC，换算偏移 {时区偏移}h。

# 1. 结论摘要

{逐条结论，一条一行、以 • 开头；每条都必须带具体数值}

# 2. 数据与方法

## 2.1 数据来源

{每个模型一条，以 + 开头；每条写明模型名、归档目录名与起报个例数}

**逐时效有效样本数**

| **预报时效（h）** | {各评估时效各占一列，列名即时效数值} |
| --- | {每个时效补一列 ---} |
| 样本数 | {各时效下的有效起报个例数} |

{样本说明句}

## 2.2 评估指标

总体路径误差直接使用 CSV 中的 `track_err_km`，按模型和时效计算算术平均值。该指标是预报位置与实况位置之间的非负距离，数值越小越好。

其余指标使用 CSV 中已有的误差字段，并计算平均绝对误差（MAE）：

$$\mathrm{MAE}=\frac{1}{n}\sum_{i=1}^{n}|e_i|$$

| **指标** | **CSV 字段** | **单位** |
| --- | --- | --- |
| 沿轨误差 | `at_km` | km |
| 横轨误差 | `ct_km` | km |
| 最大风速误差 | `wind_err_ms` | m/s |
| 最低气压误差 | `pmin_err_hpa` | hPa |

{MAE 口径说明句}

# 3. 总体路径误差

**{模型数}模型总体路径误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，按模型名字典序；每个时效的最小值加粗}

![track_error](track_error.png)

图 1：{图注}

**主要特征**

{逐条特征，一条一行、以数字编号开头；每条都必须带具体数值，并点明该时效的样本数}

# 4. 分量误差与强度误差

## 4.1 沿轨误差 MAE

**沿轨误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{沿轨判读句}

## 4.2 横轨误差 MAE

**横轨误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{横轨判读句}

## 4.3 最大风速误差 MAE

**最大风速误差 MAE（m/s）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{风速判读句}

## 4.4 最低气压误差 MAE

**最低气压误差 MAE（hPa）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{气压判读句}

![error_mae](error_mae.png)

图 2：{图注}

# 5. 综合结论

{逐条结论，一条一行、以数字编号开头、首句加粗；每条都必须带具体数值}

# 6. 局限性

{逐条局限，以 + 开头；至少覆盖样本量随时效衰减、样本非独立、MAE 不保留方向、无显著性检验四条}

# 7. 后续建议

{逐条建议，一条一行、以数字编号开头}
````
