# 集合台风报告骨架

覆盖 `weather_typhoon_ens_<模型>` 跑出来的逐起报归档：每个场次三份文件——
`tc<编号>_<起报时刻>_ensemble.csv`（逐时效的集合汇总）、
`tc<编号>_<起报时刻>_members.csv`（逐成员的原始误差）、`_meta.json`。

同一 config 家族还有一份：[`weather_typhoon_single.md`](./weather_typhoon_single.md)（单成员）。
**先读那一份**——章节编号、图注契约、占位符写法完全一致，这里只写**不一样的地方**。

**照这个填空就是最终报告。** `{…}` 是占位符，**其余每一个字逐字照抄，不许改**。

## 与单成员那份的三处差别

1. **误差口径要显式声明**。集合有两种算法，数值不可混用，报告头必须写明用的是哪种：

   | 方案 | `ens_agg` | 口径 | 逐时效误差字段 |
   | --- | --- | --- | --- |
   | A | `"A"` | **先误差后平均**：各成员各自与实况求误差，再对成员取平均 | `track_err_mean_km` |
   | B | `"B"` | **先平均后误差**：先对各成员的位置求集合平均，再与实况求误差（与 `recompute_ens_track_error.py` 同口径，**默认**） | `track_err_mean_km` |

   两种方案落到表里的**列名相同**，靠 `_meta.json` 的 `aggregation` 字段区分，
   **报告里必须用文字点明是哪一种**，否则数字无法复现。

2. **汇总表没有风速项**。集合汇总表只有 `n_members`、`n_valid_members`、
   `track_err_mean_km`、`pmin_err_mean_hpa`、`at_mean_km`、`ct_mean_km`；
   **最大风速误差只在逐成员的 `_members.csv` 里**（`wind_err_ms`）。
   报告里要给风速就得以成员为单位统计，并在表头写明是"逐成员 MAE"，
   不能和单成员报告的那张表并排放进同一个对比。

3. **成员完成情况要交代**。集合是进程池并行跑的，成员可能失败。
   `_meta.json` 的 `n_members_found` / `n_members_completed` / `failed_members`
   必须进报告——只跑成一半成员的集合，均值的含义和跑全的完全不同。

**编号契约**：`图 N：` 必须是 `1..3` 连续；每张图必须在正文里出现过。

> **本骨架目前没有测试守着**，而且**还没有任何一次集合台风跑批产出过数据**
> ——下面的列名全部来自 `core/tc_ref.py` 的集合分支代码，不是实拍。
> 第一次真跑之后要拿真实表头回来核一遍。

---

<!-- skeleton: E -->
````markdown
# XMETAI {模型数}模型台风路径与强度预报误差检验报告（集合）

**归档：{归档名}**

报告日期：{报告日期}    评估层级：输出级复核

## 报告定位与衔接

报告类型：台风路径/强度检验（typhoon/ens）。预测目标是**诊断出来的台风中心位置**与**中心强度**，不是格点场。{衔接说明}

本报告的集合误差采用**{方案名}**：{方案说明句}。报告中的路径误差为 `track_err_mean_km`（km），不做相对化。中心由各成员预报场 msl 各自链式诊断得到（前 {初期小时}h 用 ±{初期搜索半径}° 方框、其后用 ±{搜索半径}° 方框，从起报时刻实况位置出发逐时效推进）；强度取当前诊断中心 ±{强度半径}° 方框内 10 m 风速最大值。

实况来源是 babj diamond7 报文，时间为**北京时**（UTC+8）；预报起报时刻为 UTC，换算偏移 {时区偏移}h。

# 1. 结论摘要

{逐条结论，一条一行、以 • 开头；每条都必须带具体数值}

# 2. 数据与方法

## 2.1 数据来源

{每个模型一条，以 + 开头；每条写明模型名、归档目录名、起报个例数与成员数}

**成员完成情况**

| **模型** | **起报个例数** | **每例成员数** | **完成成员数** | **失败成员** |
| --- | --- | --- | --- | --- |
{一行一个模型，按模型名字典序；每例成员数不一致时写范围}

{成员完成情况说明句}

**逐时效有效样本数**

| **预报时效（h）** | {各评估时效各占一列，列名即时效数值} |
| --- | {每个时效补一列 ---} |
| 样本数 | {各时效下的有效起报个例数} |

{样本说明句}

## 2.2 评估指标

总体路径误差使用 `_ensemble.csv` 的 `track_err_mean_km`，按模型和时效计算算术平均值，数值越小越好。

分量误差与气压误差同样来自集合汇总表；风速误差来自 `_members.csv`，按成员统计成 MAE 后再对成员取平均：

$$\mathrm{MAE}=\frac{1}{n}\sum_{i=1}^{n}|e_i|$$

| **指标** | **数据来源** | **字段** | **单位** |
| --- | --- | --- | --- |
| 沿轨误差 | `_ensemble.csv` | `at_mean_km` | km |
| 横轨误差 | `_ensemble.csv` | `ct_mean_km` | km |
| 最低气压误差 | `_ensemble.csv` | `pmin_err_mean_hpa` | hPa |
| 最大风速误差 | `_members.csv` | `wind_err_ms` | m/s |

{MAE 口径说明句；点明风速项是逐成员口径，与单成员报告的同名表不可直接并排}

# 3. 总体路径误差

**{模型数}模型总体路径误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，按模型名字典序；每个时效的最小值加粗}

![track_error](track_error.png)

图 1：{图注}

**主要特征**

{逐条特征，一条一行、以数字编号开头；每条都必须带具体数值，并点明该时效的样本数}

# 4. 分量误差与强度误差

## 4.1 沿轨误差 MAE

**沿轨误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{沿轨判读句}

## 4.2 横轨误差 MAE

**横轨误差 MAE（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{横轨判读句}

## 4.3 最低气压误差 MAE

**最低气压误差 MAE（hPa）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{气压判读句}

## 4.4 最大风速误差 MAE（逐成员口径）

**最大风速误差 MAE（m/s，逐成员）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序}

{风速判读句；点明与单成员报告的同名表口径不同}

![error_mae](error_mae.png)

图 2：{图注}

# 5. 集合离散度与成员一致性

**逐时效成员路径误差离散度（km）**

| **模型** | {各评估时效各占一列} |
| --- | {每个时效补一列 ---} |
{一行一个模型，与第 3 节同序；填各时效的成员误差标准差或分位距，表头写明用的是哪个}

{离散度判读句；对照第 3 节的集合平均误差，说明离散度与误差是否同步增长}

![member_spread](member_spread.png)

图 3：{图注}

# 6. 综合结论

{逐条结论，一条一行、以数字编号开头、首句加粗；每条都必须带具体数值}

# 7. 局限性

{逐条局限，以 + 开头；至少覆盖样本量随时效衰减、样本非独立、成员失败、MAE 不保留方向、无显著性检验五条}

# 8. 后续建议

{逐条建议，一条一行、以数字编号开头}
````
