# 集合降水分类检验报告骨架

覆盖 `weather_ts_ens` 一条流程，渲染器与确定性那份共用
（`visualization/ts_report.py`），输入同样是产物目录里的
`diagnostics/categorical_wide.csv`。确定性两条流程（`weather_ts_det` /
`fdp_precip_ts`）是 [`weather_ts_single.md`](./weather_ts_single.md)——
**集合单独成篇**，因为除了成员口径，这一份还多一整节概率评分。

**照这个填空就是最终报告。** `{…}` 是占位符，**其余每一个字逐字照抄，不许改**——
包括标点、加粗、`>` 引用块、表头与分隔行、图注与表题的位置。

## 与 `weather_ts_single.md` 的差别

章节写法、编号规则、二～五节的内容**完全一致**，差别只有两处：

- **口径行多了集合口径**：头部多一行「集合成员数」，并声明 TS / POD / FAR / 漏报率 /
  BIAS **都在集合成员平均场（ensemble mean）上计算**——是确定性口径的评分，
  不是逐成员评分后再平均。成员之间的离散信息不进入这五个指标；
- **比确定性那份多一节「六、概率评分（AROC / BSS）」**：成员分布终于用上了，
  AROC 看区分能力、BSS 看相对气候基准的技巧。因为它插在第五节之后，
  后面「改进建议 / 口径与注意事项」以及对比节、雷达图节的**章节号整体后移一位**。

概率评分那一节**时间窗与阈值都和 TS 不同**（TS 是 24h 累积窗、阈值
0.1/10/25/50/100/250mm；AROC/BSS 另有自己的窗与阈值，本仓库现用的 6h 窗是
0.1/4/13/25mm）。两套口径既不可比、也不要画进同一张图——这正是本节
**单独立节、单独编号**的理由。

报告按**论文实验章节**写：每一节都是「引导句 → 图表 → 小结」。
**图注在图下方、表题在表上方**（中文论文的排版惯例），图和表按在报告里出现的顺序
连续编号（`图 1`…`图 N`、`表 1`…`表 N`）。

三份骨架：单模型（默认）、有对比对象（单基准）、有对比对象（多模型）。
二 / 三 / 四 / 五 / **六**这五节**三个分支完全一样**，差别在对比节、雷达图节和
后面的章节号。

**模型能力雷达图只在有对比对象时出现**（单模型画一个多边形没有比较对象），
所以骨架 A 没有那一节；B 里它是第八节（图 10），C 里也是第八节（图 11——
B 没有那张多模型差值图，号比 C 少一个）。轴与口径同 `weather_ts_single.md`，
两边的这一节**逐字相同**，改一处就要改两处。

**编号契约**：骨架 A 是 `图 1..9` + `表 1..4`，骨架 B 是 `图 1..10` + `表 1..5`，
骨架 C 是 `图 1..11` + `表 1..5`；编号必须连续、不跳号不重号，每张图都必须在正文里
出现过。

**末尾的「附 L」是可选的**（有没有分纬度带结果都占这一节），它的图走**独立编号
空间 `图 L1`**，不进正文 `图 1..N` 的连续性检查——这样加不加这一块，正文的图号
一个都不动。带名与边界**照搬配置**，报告不翻译也不写死任何带名。写法与
`weather_ts_single.md` 那一块**逐字相同**，改一处就要改两处。

> **本骨架目前没有测试守着**（`tests/` 在这个仓库里是空的，见 `README.md`
> 的「怎么被保证」）。`weather_ts_single.md` 有 `tests/unit/visualization/test_ts_report.py`
> 逐字扫描，这一份**没有**；改 `ts_report.py` 的渲染时得自己回来对一遍骨架，
> 别指望 CI 红给你看。
> **占位符不要跨行写**——`{` 和 `}` 两边都算占位符标记。

> **第六节目前没有代码支撑，缺两处：**
> 1. **该能力不产 AROC/BSS 表**——`core/api.py` 的 `run_ts_ens` 把
>    `window_aroc` 置成空元组，只算 TS 那一套；
> 2. **渲染器没有这个分支**——真正算 AROC/BSS 的是独立能力 `weather_ts_ens_prob`，
>    它**不在** `visualization/report.py` 的 `IMPLEMENTED` 里，`README.md` 里也标着
>    `[BLOCKED]`（依赖的 `vfc.reader_categorical` / `station_categorical` /
>    `metric_categorical` 在参考源里缺失）。AROC/BSS 的取数与绘图要新写。
>
> 数**据**本身是有的：集合跑的 `aroc_bss_<name>.csv` 会跟 `ts_<name>.csv` 落在一起，
> 列是 `window_h, lead_h, grade, threshold_mm, AROC, BS, BS_ref, BSS, base_rate,
> n_points, n_used`，每个时效除了逐阈值行还有一行 `grade="AVG"` 的综合档。
> 不填这一节就**整节删掉**，别留空表——删了记得把后面章节号和图号表号一起往前收。
>
> **连带影响**：真报告（`ts_report.py`）**现在不出第六节**，所以同一份报告里
> 「模型能力雷达图」的章节号与图号都比本骨架**少 1**——骨架 B 写的是第八节 / 图 10，
> 集合真跑出来是第七节 / 图 9（骨架 C 是第八节 / 图 11 对第七节 / 图 10）。
> 这是第六节缺席带来的整体位移，不是渲染器漏了谁；第六节补上就自动对齐。

---

## 骨架 A：单模型（默认）

用 `generate_report.py` 跑出来的就是这一份。

<!-- skeleton: A -->
````markdown
# 集合预报{模型数}模型降水评估报告

## 一、结论摘要

本报告评估{模型名}（来源：`{来源文件路径}`），集合每例{成员数}个成员，{对比对象说明}评估口径为站点分类检验（{窗口}h 累积窗口，时效 {首时效}–{末时效}h 共 {时效个数} 个，{记录条数} 条记录），指标为 TS、POD、FAR、漏报率与 BIAS。本次改动：{本次改动说明}。报告生成时间：{生成时间}。

{结论，一整段、不分点；把每条判断连成通顺的连续文字，每条都必须带具体数值}

**业务定性**：{业务定性}

集合口径：TS / POD / FAR / 漏报率 / BIAS 均在{成员数}个成员的**平均场**上计算（确定性口径），不是逐成员评分后再平均；成员离散信息只在第六节的概率评分里体现。

指标定义：TS = hits/(hits+misses+false_alarms)，POD = hits/(hits+misses)，FAR = false_alarms/(hits+false_alarms)，漏报率 = 1−POD，BIAS = (hits+false_alarms)/(hits+misses)（频率偏差）

## 二、逐降水等级表现（全时效平均）

下表汇总各降水等级在整个时效范围内的表现，用于判断模型在不同量级上的技巧水平与偏差结构。

表 1：{表题}

| 降水等级 | 阈值(mm) | TS | POD | FAR | 漏报率 | BIAS | TS(等权平均) | 命中 | 漏报 | 空报 | 样本合计 |
|---|---|---|---|---|---|---|---|---|---|---|---|
{一行一个降水等级，按阈值升序；TS 后面跟 🟢 / 🟡 / 🔴 分级标记；无效值写 —}

> 全时效平均为**各时效等权平均**（不是按样本数加权）；判断强降水能力请看 ≥25mm 及以上等级。

![metrics_by_threshold]({窗口}h_metrics_by_threshold_{基准时效}h.png)

图 1：{图注——说明怎么读这张图}

**小结**：{量级维度的定量分析：分级分布、是否单调、相邻量级落差最大的一档、可用量级上限、样本随量级的收缩}

## 三、TS 随时效变化（{首时效}–{末时效}h）

按预报时效展开，用于确定该模型的有效预报时效——技巧掉到不可用之前能推多远。

![TS_vs_lead]({窗口}h_TS_vs_lead.png)

图 2：{图注}

![TS_heatmap]({窗口}h_TS_heatmap.png)

图 3：{图注}

表 2：{表题}

| 时效(h) | {一个降水等级一列} |
|---|{每个降水等级补一列 ---|}
{一行一个时效，按升序；单元格填该时效该等级的 TS}

> 单元格是该时效该等级的 TS；同一行内横向比较看衰减速度，同一列内纵向比较看量级差异。

**小结**：{时效维度的定量分析：各量级的有效时效上限、是否单调、是否进入平台期、整体衰减速度、衰减最快的量级}

## 四、误差形态与偏差结构

技巧高低之外还要看误差从哪来：本节从命中率、空报率与频率偏差三个方面拆解误差的来源与形态。

![POD_vs_lead]({窗口}h_POD_vs_lead.png)

图 4：{图注}

![FAR_vs_lead]({窗口}h_FAR_vs_lead.png)

图 5：{图注}

![BIAS_vs_lead]({窗口}h_BIAS_vs_lead.png)

图 6：{图注}

![performance_diagram]({窗口}h_performance_diagram_{基准时效}h.png)

图 7：{图注}

**小结**：{误差形态的定量分析：空报与漏报谁主导、在哪一档发生反转、BIAS 随量级的走向、最接近理想值的量级}

## 五、跨时效分布（箱线图，TS）

把同一模型、同一量级在各时效上的 TS 视为一个样本，用箱线图刻画其离散程度——平均技巧相同的两个模型，稳定性可能差很远。

表 3：{表题}

| 模型 | 降水等级 | 中位数 | IQR | Q1–Q3 | 须线范围 | 离群时效 | 时效数 |
|---|---|---|---|---|---|---|---|
{一行一个「模型 × 降水等级」；量级升序，同量级内本模型在前；某一格没有有效值时全列写 —、时效数写 0}

> 每个箱体的样本是**同模型、同量级在全部时效上的 TS 取值**（无有效 TS 的时效不计入，样本数见「时效数」列）：箱体为四分位距（IQR），中线为中位数，须线延伸到 1.5×IQR 内最远的点，空心圆为离群时效。**TS 越高越优**，所以箱体**越高**表示该量级整体技巧越好、**越窄**表示跨时效越稳定；这里的统计量与第二节的「全时效平均」不是同一个量（等权平均 vs 中位数/IQR），不可互相替代。

![TS_boxplot]({窗口}h_TS_boxplot.png)

图 8：{图注}

**小结**：{稳定性的定量分析：各模型 IQR 排序、离群时效落在短时效还是长时效、中位数与等权平均是否给出不同的领先者}

## 六、概率评分（AROC / BSS，{概率窗口}h 累积窗口）

前面五节评的都是{成员数}个成员的**平均场**，成员之间的离散信息一点没进指标——一个成员高度一致、另一个成员四散分布的集合，前五节的分数可以完全相同。本节改用全体成员的概率分布：AROC 衡量预报概率把事件与非事件分开的能力（0.5 为无技巧），BSS 衡量相对气候基准的 Brier 技巧（>0 才算有正技巧）。

**本节与前面五节不同口径**：概率评分用 {概率窗口}h 累积窗口、阈值 {概率阈值列表}，与前面 {窗口}h / {TS阈值列表} 既不可比，也不要画进同一张图。

表 4：{表题}

| 降水等级 | 阈值(mm) | AROC | BS | BS_ref | BSS | 基准率 | 样本点数 |
|---|---|---|---|---|---|---|---|
{一行一个概率评分的降水等级，按阈值升序；末尾一行是 AVG 综合档（阈值/基准率/样本点数写 — 与参与平均的档数）；无效值写 —}

> BS_ref 是气候基准的 Brier 分数，**只有外部给了气候概率参考的 6h 窗才有值**，其余窗口整列写 —；BSS = 1 − BS/BS_ref。AROC 在**逐成员插值后**按超越式阈值（x ≥ 阈值）计算，与前面章节"插值到站再配对"的做法不是一回事。AVG 行是对**可算阈值**的等权平均（该档无事件的跳过），不是按样本数加权。

![aroc_bss_vs_lead]({概率窗口}h_AROC_BSS_vs_lead.png)

图 9：{图注}

**小结**：{概率评分的定量分析：各档 AROC 距 0.5 的余量、BSS 的正负与随量级的走向、高阈值档的样本量是否还撑得住、AVG 档随时效的衰减}

## 七、改进建议

{逐条建议，一条一行、编号从 1 开始}

## 八、口径与注意事项

- **值缺失的含义**：TS 为 “—” 表示该等级没有有效配对或无事件，与“评分为 0”不同；
- **BIAS 不是平均误差**：它是频率偏差，>1 表示预报事件偏多，<1 表示偏少；
- **前五节评的是集合平均场**：TS / POD / FAR / 漏报率 / BIAS 都不含成员离散信息，集合的相对优势要看第六节；
- **概率评分与 TS 不可混比**：两者的累积窗口与阈值都不同，AROC 还是逐成员概率口径；
- **跨时效不要直接平均后比较模型**：正式对比应使用共同有效样本与固定验证口径；
- 结果来自统一长表（`scores.csv` / `categorical_wide.csv`，概率评分另见 `aroc_bss_<name>.csv`），逐行可追溯到具体时效与阈值。

## 附 L 分纬度带结果（可选）

本块把宽表里 `region` 非空的行**单独汇总**，全球行**不参与**——全球平均与
纬度带平均是两个量，混在一起算会把带间差异整个抹平，这一块也就没有意义了。
带名与边界照搬评测配置 `options["regions"]`：**报告不翻译、也不写死任何带名**，
配置里叫 `south` 就显示 `south`；要换一套切法改配置重跑即可，报告这边一个字
都不用动。表里的口径与第二节完全一致（先合计列联表再算比率，再对等级等权平均），
末列的「综合」就是该带自己的全等级等权平均。

**分纬度带 TS（各降水等级，全时效平均）**

| **纬度带** | {每个降水等级各占一列，列名照抄第二节的等级写法} | **综合** |
|---|{每个降水等级各补一列 ---|}---|
{一行一个纬度带，按南界从低到高排（取不到边界时按带名升序）；带名写成 `south [0, 15]` 这种「带名 + 边界」，边界取自 manifest 的 `regions`，取不到就只写带名；末列是该带的全等级等权平均；无效值写 —}

{分带判读句：最强的带、最弱的带、相差多少；数值并列就写并列（判定按表里显示的三位小数），并写明头名是否随预报时效翻转}

> 这一块**只汇总主模型**：按带切和按模型切是两个正交的维度，硬塞进同一张表，
> 会出现「同一个名字既是带名又是模型名」的歧义。另外，各带的站点数差得远时
> TS 的抽样噪声也不一样，差一两个百分点不必当结论。

![region_TS_vs_lead]({窗口}h_region_TS_vs_lead.png)

图 L1：{图注——各纬度带 TS 随时效变化}
````

---

## 骨架 B：有对比对象（单基准）

与骨架 A 的差别只有两处：多一行**可比性**、**第七节**多一张对比表，
后面的建议/口径**各往后挪一位**（八 / 九）。

> 对比对象多于一个时走骨架 C——单基准只是"一个对比对象"的特例，表体换成差值三列。
> 这一分支没有多模型差值图（`multi_model_TS_delta` 只在给了多个对比对象时生成），
> 所以第七节**只有表、没有图**。

<!-- skeleton: B -->
````markdown
# 集合预报{模型数}模型降水评估报告

## 一、结论摘要

本报告评估{模型名}（来源：`{来源文件路径}`），集合每例{成员数}个成员，{对比对象说明}评估口径为站点分类检验（{窗口}h 累积窗口，时效 {首时效}–{末时效}h 共 {时效个数} 个，{记录条数} 条记录），指标为 TS、POD、FAR、漏报率与 BIAS。本次改动：{本次改动说明}。报告生成时间：{生成时间}。

{结论，一整段、不分点；把每条判断连成通顺的连续文字，每条都必须带具体数值}

**业务定性**：{业务定性}

集合口径：TS / POD / FAR / 漏报率 / BIAS 均在{成员数}个成员的**平均场**上计算（确定性口径），不是逐成员评分后再平均；成员离散信息只在第六节的概率评分里体现。

指标定义：TS = hits/(hits+misses+false_alarms)，POD = hits/(hits+misses)，FAR = false_alarms/(hits+false_alarms)，漏报率 = 1−POD，BIAS = (hits+false_alarms)/(hits+misses)（频率偏差）

## 二、逐降水等级表现（全时效平均）

下表汇总各降水等级在整个时效范围内的表现，用于判断模型在不同量级上的技巧水平与偏差结构。

表 1：{表题}

| 降水等级 | 阈值(mm) | TS | POD | FAR | 漏报率 | BIAS | TS(等权平均) | 命中 | 漏报 | 空报 | 样本合计 |
|---|---|---|---|---|---|---|---|---|---|---|---|
{一行一个降水等级，按阈值升序；TS 后面跟 🟢 / 🟡 / 🔴 分级标记；无效值写 —}

> 全时效平均为**各时效等权平均**（不是按样本数加权）；判断强降水能力请看 ≥25mm 及以上等级。

![metrics_by_threshold]({窗口}h_metrics_by_threshold_{基准时效}h.png)

图 1：{图注——说明怎么读这张图}

**小结**：{量级维度的定量分析：分级分布、是否单调、相邻量级落差最大的一档、可用量级上限、样本随量级的收缩}

## 三、TS 随时效变化（{首时效}–{末时效}h）

按预报时效展开，用于确定该模型的有效预报时效——技巧掉到不可用之前能推多远。

![TS_vs_lead]({窗口}h_TS_vs_lead.png)

图 2：{图注}

![TS_heatmap]({窗口}h_TS_heatmap.png)

图 3：{图注}

表 2：{表题}

| 时效(h) | {一个降水等级一列} |
|---|{每个降水等级补一列 ---|}
{一行一个时效，按升序；单元格填该时效该等级的 TS}

> 单元格是该时效该等级的 TS；同一行内横向比较看衰减速度，同一列内纵向比较看量级差异。

**小结**：{时效维度的定量分析：各量级的有效时效上限、是否单调、是否进入平台期、整体衰减速度、衰减最快的量级}

## 四、误差形态与偏差结构

技巧高低之外还要看误差从哪来：本节从命中率、空报率与频率偏差三个方面拆解误差的来源与形态。

![POD_vs_lead]({窗口}h_POD_vs_lead.png)

图 4：{图注}

![FAR_vs_lead]({窗口}h_FAR_vs_lead.png)

图 5：{图注}

![BIAS_vs_lead]({窗口}h_BIAS_vs_lead.png)

图 6：{图注}

![performance_diagram]({窗口}h_performance_diagram_{基准时效}h.png)

图 7：{图注}

**小结**：{误差形态的定量分析：空报与漏报谁主导、在哪一档发生反转、BIAS 随量级的走向、最接近理想值的量级}

## 五、跨时效分布（箱线图，TS）

把同一模型、同一量级在各时效上的 TS 视为一个样本，用箱线图刻画其离散程度——平均技巧相同的两个模型，稳定性可能差很远。

表 3：{表题}

| 模型 | 降水等级 | 中位数 | IQR | Q1–Q3 | 须线范围 | 离群时效 | 时效数 |
|---|---|---|---|---|---|---|---|
{一行一个「模型 × 降水等级」；量级升序，同量级内本模型在前；某一格没有有效值时全列写 —、时效数写 0}

> 每个箱体的样本是**同模型、同量级在全部时效上的 TS 取值**（无有效 TS 的时效不计入，样本数见「时效数」列）：箱体为四分位距（IQR），中线为中位数，须线延伸到 1.5×IQR 内最远的点，空心圆为离群时效。**TS 越高越优**，所以箱体**越高**表示该量级整体技巧越好、**越窄**表示跨时效越稳定；这里的统计量与第二节的「全时效平均」不是同一个量（等权平均 vs 中位数/IQR），不可互相替代。

![TS_boxplot]({窗口}h_TS_boxplot.png)

图 8：{图注}

**小结**：{稳定性的定量分析：各模型 IQR 排序、离群时效落在短时效还是长时效、中位数与等权平均是否给出不同的领先者}

## 六、概率评分（AROC / BSS，{概率窗口}h 累积窗口）

前面五节评的都是{成员数}个成员的**平均场**，成员之间的离散信息一点没进指标——一个成员高度一致、另一个成员四散分布的集合，前五节的分数可以完全相同。本节改用全体成员的概率分布：AROC 衡量预报概率把事件与非事件分开的能力（0.5 为无技巧），BSS 衡量相对气候基准的 Brier 技巧（>0 才算有正技巧）。

**本节与前面五节不同口径**：概率评分用 {概率窗口}h 累积窗口、阈值 {概率阈值列表}，与前面 {窗口}h / {TS阈值列表} 既不可比，也不要画进同一张图。

表 4：{表题}

| 降水等级 | 阈值(mm) | AROC | BS | BS_ref | BSS | 基准率 | 样本点数 |
|---|---|---|---|---|---|---|---|
{一行一个概率评分的降水等级，按阈值升序；末尾一行是 AVG 综合档（阈值/基准率/样本点数写 — 与参与平均的档数）；无效值写 —}

> BS_ref 是气候基准的 Brier 分数，**只有外部给了气候概率参考的 6h 窗才有值**，其余窗口整列写 —；BSS = 1 − BS/BS_ref。AROC 在**逐成员插值后**按超越式阈值（x ≥ 阈值）计算，与前面章节"插值到站再配对"的做法不是一回事。AVG 行是对**可算阈值**的等权平均（该档无事件的跳过），不是按样本数加权。

![aroc_bss_vs_lead]({概率窗口}h_AROC_BSS_vs_lead.png)

图 9：{图注}

**小结**：{概率评分的定量分析：各档 AROC 距 0.5 的余量、BSS 的正负与随量级的走向、高阈值档的样本量是否还撑得住、AVG 档随时效的衰减}

## 七、与基准 {基准名} 的对比（{基准时效}h）

以下比较在同一时效、同一阈值口径下进行。

表 5：{表题}

| 降水等级 | 本模型 TS | 对比模型 TS | 差值 |
|---|---|---|---|
{一行一个降水等级；差值列带 ↑ / ↓ / ≈ 前缀}

**小结**：{对比分析：谁在哪些量级领先、名次是否随量级翻转、差距最大与最小的量级、本模型平均名次}

## 八、模型能力雷达图

这一节把前面几张表的**综合评分**摆成多边形：每根轴一种能力，每个模型一个
多边形，**越靠外越好**。它不引入任何新算法，只是把前面的数换个读法——
看的是「能力形状」，不是「谁排第一」：两个模型综合分接近时，雷达图能显出
差距集中在哪几项上。

共 {轴数} 根轴，方向已在括号里标明（越高越好 / 越低越好）：{轴列表}。

{若有轴被剔除，一行写明剔了哪几根、为什么（缺值整根剔除，不拿 0 顶替）；没有就整行删掉}

**读法**：纵轴 0–1 是**批内相对分**，每根轴上 1 = 本批最好、0 = 本批最差
（各轴方向先行统一）。这是{模型数}模型互比出来的相对位置，**不是绝对能力分**
——全批都差时每根轴照样有人拿 1；只有两个模型时必然是 1 和 0。某一根轴上所有
模型打平时，该轴一律画在满格 1.0（画成 0 会让多边形凭空凹进去），所以**满格
不代表领先**，要回前面几节看绝对数值。

![capability_radar]({窗口}h_capability_radar.png)

图 10：{模型数}模型能力雷达图（{轴数} 根综合评分轴；各轴批内 min-max 归一化，1 = 本批最好）

{末句：各轴得分之和最大与最小的模型；两个模型数值完全一样时不写}

## 九、改进建议

{逐条建议，一条一行、编号从 1 开始}

## 十、口径与注意事项

- **值缺失的含义**：TS 为 “—” 表示该等级没有有效配对或无事件，与“评分为 0”不同；
- **BIAS 不是平均误差**：它是频率偏差，>1 表示预报事件偏多，<1 表示偏少；
- **前五节评的是集合平均场**：TS / POD / FAR / 漏报率 / BIAS 都不含成员离散信息，集合的相对优势要看第六节；
- **概率评分与 TS 不可混比**：两者的累积窗口与阈值都不同，AROC 还是逐成员概率口径；
- **跨时效不要直接平均后比较模型**：正式对比应使用共同有效样本与固定验证口径；
- 结果来自统一长表（`scores.csv` / `categorical_wide.csv`，概率评分另见 `aroc_bss_<name>.csv`），逐行可追溯到具体时效与阈值。

## 附 L 分纬度带结果（可选）

本块把宽表里 `region` 非空的行**单独汇总**，全球行**不参与**——全球平均与
纬度带平均是两个量，混在一起算会把带间差异整个抹平，这一块也就没有意义了。
带名与边界照搬评测配置 `options["regions"]`：**报告不翻译、也不写死任何带名**，
配置里叫 `south` 就显示 `south`；要换一套切法改配置重跑即可，报告这边一个字
都不用动。表里的口径与第二节完全一致（先合计列联表再算比率，再对等级等权平均），
末列的「综合」就是该带自己的全等级等权平均。

**分纬度带 TS（各降水等级，全时效平均）**

| **纬度带** | {每个降水等级各占一列，列名照抄第二节的等级写法} | **综合** |
|---|{每个降水等级各补一列 ---|}---|
{一行一个纬度带，按南界从低到高排（取不到边界时按带名升序）；带名写成 `south [0, 15]` 这种「带名 + 边界」，边界取自 manifest 的 `regions`，取不到就只写带名；末列是该带的全等级等权平均；无效值写 —}

{分带判读句：最强的带、最弱的带、相差多少；数值并列就写并列（判定按表里显示的三位小数），并写明头名是否随预报时效翻转}

> 这一块**只汇总主模型**：按带切和按模型切是两个正交的维度，硬塞进同一张表，
> 会出现「同一个名字既是带名又是模型名」的歧义。另外，各带的站点数差得远时
> TS 的抽样噪声也不一样，差一两个百分点不必当结论。

![region_TS_vs_lead]({窗口}h_region_TS_vs_lead.png)

图 L1：{图注——各纬度带 TS 随时效变化}
````

---

## 骨架 C：有对比对象（多模型）

`--compare 名字=CSV路径`（可重复）走的就是这一份。与骨架 B 的差别只有第七节：
不是"本模型 vs 一个基准"的两列表，而是"本模型 + 各对比模型 + 本模型名次"的横表，
并且**多一张差值图**（每个降水等级一个子图，纵轴是「对比模型 − 本模型」）。

<!-- skeleton: C -->
````markdown
# 集合预报{模型数}模型降水评估报告

## 一、结论摘要

本报告评估{模型名}（来源：`{来源文件路径}`），集合每例{成员数}个成员，{对比对象说明}评估口径为站点分类检验（{窗口}h 累积窗口，时效 {首时效}–{末时效}h 共 {时效个数} 个，{记录条数} 条记录），指标为 TS、POD、FAR、漏报率与 BIAS。本次改动：{本次改动说明}。报告生成时间：{生成时间}。

{结论，一整段、不分点；把每条判断连成通顺的连续文字，每条都必须带具体数值}

**业务定性**：{业务定性}

集合口径：TS / POD / FAR / 漏报率 / BIAS 均在{成员数}个成员的**平均场**上计算（确定性口径），不是逐成员评分后再平均；成员离散信息只在第六节的概率评分里体现。

指标定义：TS = hits/(hits+misses+false_alarms)，POD = hits/(hits+misses)，FAR = false_alarms/(hits+false_alarms)，漏报率 = 1−POD，BIAS = (hits+false_alarms)/(hits+misses)（频率偏差）

## 二、逐降水等级表现（全时效平均）

下表汇总各降水等级在整个时效范围内的表现，用于判断模型在不同量级上的技巧水平与偏差结构。

表 1：{表题}

| 降水等级 | 阈值(mm) | TS | POD | FAR | 漏报率 | BIAS | TS(等权平均) | 命中 | 漏报 | 空报 | 样本合计 |
|---|---|---|---|---|---|---|---|---|---|---|---|
{一行一个降水等级，按阈值升序；TS 后面跟 🟢 / 🟡 / 🔴 分级标记；无效值写 —}

> 全时效平均为**各时效等权平均**（不是按样本数加权）；判断强降水能力请看 ≥25mm 及以上等级。

![metrics_by_threshold]({窗口}h_metrics_by_threshold_{基准时效}h.png)

图 1：{图注——说明怎么读这张图}

**小结**：{量级维度的定量分析：分级分布、是否单调、相邻量级落差最大的一档、可用量级上限、样本随量级的收缩}

## 三、TS 随时效变化（{首时效}–{末时效}h）

按预报时效展开，用于确定该模型的有效预报时效——技巧掉到不可用之前能推多远。

![TS_vs_lead]({窗口}h_TS_vs_lead.png)

图 2：{图注}

![TS_heatmap]({窗口}h_TS_heatmap.png)

图 3：{图注}

表 2：{表题}

| 时效(h) | {一个降水等级一列} |
|---|{每个降水等级补一列 ---|}
{一行一个时效，按升序；单元格填该时效该等级的 TS}

> 单元格是该时效该等级的 TS；同一行内横向比较看衰减速度，同一列内纵向比较看量级差异。

**小结**：{时效维度的定量分析：各量级的有效时效上限、是否单调、是否进入平台期、整体衰减速度、衰减最快的量级}

## 四、误差形态与偏差结构

技巧高低之外还要看误差从哪来：本节从命中率、空报率与频率偏差三个方面拆解误差的来源与形态。

![POD_vs_lead]({窗口}h_POD_vs_lead.png)

图 4：{图注}

![FAR_vs_lead]({窗口}h_FAR_vs_lead.png)

图 5：{图注}

![BIAS_vs_lead]({窗口}h_BIAS_vs_lead.png)

图 6：{图注}

![performance_diagram]({窗口}h_performance_diagram_{基准时效}h.png)

图 7：{图注}

**小结**：{误差形态的定量分析：空报与漏报谁主导、在哪一档发生反转、BIAS 随量级的走向、最接近理想值的量级}

## 五、跨时效分布（箱线图，TS）

把同一模型、同一量级在各时效上的 TS 视为一个样本，用箱线图刻画其离散程度——平均技巧相同的两个模型，稳定性可能差很远。

表 3：{表题}

| 模型 | 降水等级 | 中位数 | IQR | Q1–Q3 | 须线范围 | 离群时效 | 时效数 |
|---|---|---|---|---|---|---|---|
{一行一个「模型 × 降水等级」；量级升序，同量级内本模型在前；某一格没有有效值时全列写 —、时效数写 0}

> 每个箱体的样本是**同模型、同量级在全部时效上的 TS 取值**（无有效 TS 的时效不计入，样本数见「时效数」列）：箱体为四分位距（IQR），中线为中位数，须线延伸到 1.5×IQR 内最远的点，空心圆为离群时效。**TS 越高越优**，所以箱体**越高**表示该量级整体技巧越好、**越窄**表示跨时效越稳定；这里的统计量与第二节的「全时效平均」不是同一个量（等权平均 vs 中位数/IQR），不可互相替代。

![TS_boxplot]({窗口}h_TS_boxplot.png)

图 8：{图注}

**小结**：{稳定性的定量分析：各模型 IQR 排序、离群时效落在短时效还是长时效、中位数与等权平均是否给出不同的领先者}

## 六、概率评分（AROC / BSS，{概率窗口}h 累积窗口）

前面五节评的都是{成员数}个成员的**平均场**，成员之间的离散信息一点没进指标——一个成员高度一致、另一个成员四散分布的集合，前五节的分数可以完全相同。本节改用全体成员的概率分布：AROC 衡量预报概率把事件与非事件分开的能力（0.5 为无技巧），BSS 衡量相对气候基准的 Brier 技巧（>0 才算有正技巧）。

**本节与前面五节不同口径**：概率评分用 {概率窗口}h 累积窗口、阈值 {概率阈值列表}，与前面 {窗口}h / {TS阈值列表} 既不可比，也不要画进同一张图。

表 4：{表题}

| 降水等级 | 阈值(mm) | AROC | BS | BS_ref | BSS | 基准率 | 样本点数 |
|---|---|---|---|---|---|---|---|
{一行一个概率评分的降水等级，按阈值升序；末尾一行是 AVG 综合档（阈值/基准率/样本点数写 — 与参与平均的档数）；无效值写 —}

> BS_ref 是气候基准的 Brier 分数，**只有外部给了气候概率参考的 6h 窗才有值**，其余窗口整列写 —；BSS = 1 − BS/BS_ref。AROC 在**逐成员插值后**按超越式阈值（x ≥ 阈值）计算，与前面章节"插值到站再配对"的做法不是一回事。AVG 行是对**可算阈值**的等权平均（该档无事件的跳过），不是按样本数加权。

![aroc_bss_vs_lead]({概率窗口}h_AROC_BSS_vs_lead.png)

图 9：{图注}

**小结**：{概率评分的定量分析：各档 AROC 距 0.5 的余量、BSS 的正负与随量级的走向、高阈值档的样本量是否还撑得住、AVG 档随时效的衰减}

## 七、与对比模型的比较（{基准时效}h，TS）

以下比较在同一时效、同一阈值口径下进行。

表 5：{表题}

| 降水等级 | 本模型 | {各对比模型各一列} | 本模型名次 |
|---|{本模型、每个对比模型、名次，各补一个 ---|}
{一行一个降水等级；单元格填该模型在该时效的 TS}

> 名次按该时效的 TS 排序（仅统计有有效值的模型）；不同数据的起报时段/时效范围要一致才可直接比较。

![multi_model_TS_delta]({窗口}h_multi_model_TS_delta_vs_{主模型名}.png)

图 10：{图注}

**小结**：{对比分析：谁在哪些量级领先、名次是否随量级翻转、差距最大与最小的量级、本模型平均名次}

## 八、模型能力雷达图

这一节把前面几张表的**综合评分**摆成多边形：每根轴一种能力，每个模型一个
多边形，**越靠外越好**。它不引入任何新算法，只是把前面的数换个读法——
看的是「能力形状」，不是「谁排第一」：两个模型综合分接近时，雷达图能显出
差距集中在哪几项上。

共 {轴数} 根轴，方向已在括号里标明（越高越好 / 越低越好）：{轴列表}。

{若有轴被剔除，一行写明剔了哪几根、为什么（缺值整根剔除，不拿 0 顶替）；没有就整行删掉}

**读法**：纵轴 0–1 是**批内相对分**，每根轴上 1 = 本批最好、0 = 本批最差
（各轴方向先行统一）。这是{模型数}模型互比出来的相对位置，**不是绝对能力分**
——全批都差时每根轴照样有人拿 1；只有两个模型时必然是 1 和 0。某一根轴上所有
模型打平时，该轴一律画在满格 1.0（画成 0 会让多边形凭空凹进去），所以**满格
不代表领先**，要回前面几节看绝对数值。

![capability_radar]({窗口}h_capability_radar.png)

图 11：{模型数}模型能力雷达图（{轴数} 根综合评分轴；各轴批内 min-max 归一化，1 = 本批最好）

{末句：各轴得分之和最大与最小的模型；两个模型数值完全一样时不写}

## 九、改进建议

{逐条建议，一条一行、编号从 1 开始}

## 十、口径与注意事项

- **值缺失的含义**：TS 为 “—” 表示该等级没有有效配对或无事件，与“评分为 0”不同；
- **BIAS 不是平均误差**：它是频率偏差，>1 表示预报事件偏多，<1 表示偏少；
- **前五节评的是集合平均场**：TS / POD / FAR / 漏报率 / BIAS 都不含成员离散信息，集合的相对优势要看第六节；
- **概率评分与 TS 不可混比**：两者的累积窗口与阈值都不同，AROC 还是逐成员概率口径；
- **跨时效不要直接平均后比较模型**：正式对比应使用共同有效样本与固定验证口径；
- 结果来自统一长表（`scores.csv` / `categorical_wide.csv`，概率评分另见 `aroc_bss_<name>.csv`），逐行可追溯到具体时效与阈值。

## 附 L 分纬度带结果（可选）

本块把宽表里 `region` 非空的行**单独汇总**，全球行**不参与**——全球平均与
纬度带平均是两个量，混在一起算会把带间差异整个抹平，这一块也就没有意义了。
带名与边界照搬评测配置 `options["regions"]`：**报告不翻译、也不写死任何带名**，
配置里叫 `south` 就显示 `south`；要换一套切法改配置重跑即可，报告这边一个字
都不用动。表里的口径与第二节完全一致（先合计列联表再算比率，再对等级等权平均），
末列的「综合」就是该带自己的全等级等权平均。

**分纬度带 TS（各降水等级，全时效平均）**

| **纬度带** | {每个降水等级各占一列，列名照抄第二节的等级写法} | **综合** |
|---|{每个降水等级各补一列 ---|}---|
{一行一个纬度带，按南界从低到高排（取不到边界时按带名升序）；带名写成 `south [0, 15]` 这种「带名 + 边界」，边界取自 manifest 的 `regions`，取不到就只写带名；末列是该带的全等级等权平均；无效值写 —}

{分带判读句：最强的带、最弱的带、相差多少；数值并列就写并列（判定按表里显示的三位小数），并写明头名是否随预报时效翻转}

> 这一块**只汇总主模型**：按带切和按模型切是两个正交的维度，硬塞进同一张表，
> 会出现「同一个名字既是带名又是模型名」的歧义。另外，各带的站点数差得远时
> TS 的抽样噪声也不一样，差一两个百分点不必当结论。

![region_TS_vs_lead]({窗口}h_region_TS_vs_lead.png)

图 L1：{图注——各纬度带 TS 随时效变化}
````
