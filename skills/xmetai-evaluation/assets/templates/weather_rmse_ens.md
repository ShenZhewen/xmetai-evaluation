# 集合预报多模型报告骨架

覆盖 `weather_rmse_ens_<模型>` 跑出来的**评估产物目录**（`scores.csv` +
`manifest.json` + `diagnostics/`），由 `scripts/generate_ens_report.py` 一次喂 N 份
（≥2）出多模型对比报告。集合特有的指标是长表里 `crps` / `spread` /
`spread_error_ratio` 那几行（`product_kind="ensemble"`）。
确定性单成员那一份是 [`weather_rmse_single.md`](./weather_rmse_single.md)，
谱检验那一份是 [`weather_rmse_wave.md`](./weather_rmse_wave.md)。同属一个 config 家族，
章节写法一致，差别在指标族——
这一份多了集合特有的 CRPS 与离散度（Spread、Spread/RMSE），
且这里的 RMSE / ACC / FA 都是**集合平均场**口径，不是单个成员。

**照这个填空就是最终报告。** `{…}` 是占位符，**其余每一个字逐字照抄，不许改**——
包括标点、加粗、`>` 引用块、表头与分隔行、图注的位置。

与 `weather_ts_single.md` 的差别同 `weather_rmse_single.md`：这一份**不按论文实验
章节编号图与表**，章节用 `# 1.`…`# 7.`，每张表由它上方的粗体小标题引导（**表不编号**），
只有附录的六张图带 `图 N：` 图注。这样做是因为报告里有多处**条件章节**
（数据质量异常、变量覆盖缺口、检验项缺失），一旦表参与全局编号，
条件章节一消失后面全要重排。

**本骨架不含球谐带功率**，那部分是 [`weather_rmse_wave.md`](./weather_rmse_wave.md)
的活——同一份源报告里「球谐带功率」「球谐带功率比」两节，在那个骨架里对应
5.1 球谐带分频结果 / 5.2 球谐带分变量结果 / 5.3 球谐带配对检验，
这里不重复。这一份的谱只保留纬向谱。

**编号契约**：分三个互不干扰的命名空间——

- 正文与必出附录用 `图 N：`，编号从 `1` 起、不重号、不跳号到用不到的号；
  其中 `图 1`–`图 6` 是**必出**（每个批次都有对应数据），`图 7`–`图 9` 按数据
  可得性出——出不了的那一节**保留节位、写明「本批未出」及原因**，不指一张不存在
  的文件（所以这三段里出现「图 8 缺、图 9 在」是正常的，缺的是哪一节，那节的
  标题下面写着）；每张出得来的图必须在正文里出现过；
- 分纬度带可选块用 `图 L1：`…`图 L3：`，块内从 1 连续；
- 分季节可选块**预留** `图 S1：`…`图 S2：`；这一块目前本批未出，所以现在不出图、
  号留空（见文末「附 S」）。

**可选块**：报告末尾两个块按需保留——`## 附 L 分纬度带结果（可选）` 与
`## 附 S 分季节结果（可选）`。整块删掉（连它的表、图注、`图 L*`/`图 S*` 一起删）
**不需要重排任何编号**，这就是给它俩单开命名空间的原因；要用就整块留着，
不要只删图不删表。

两块的数据都来自评测产物里**同一张长表**（`region` 列 + 起报时刻），
**不是**另跑一次评测。**集合口径在这里格外要紧**：分带统计的是集合平均场还是
逐成员先算再平均，会给出不同的数，本块与正文保持同一口径（集合平均场），
并在图注里写明。拿不到数据时**保留节位并标注「本批未出」**，不静默省略、
也不留空表——与 [`weather_rmse_wave.md`](./weather_rmse_wave.md) 里球谐带那三节
同一套约定。

> **本骨架目前没有测试守着**（`tests/` 在这个仓库里是空的，见 `README.md`
> 的「怎么被保证」）。按同一套格式写、能被 `assert_matches_skeleton` 直接认，
> 但**没有**对应的 `test_weather_rmse_ens.py`；改 `ens_report.py` 的渲染时得自己回来
> 对一遍骨架，别指望 CI 红给你看。
> **占位符不要跨行写**——`{` 和 `}` 两边都算占位符标记。

---

<!-- skeleton: A -->
````markdown
# XMETAI 集合预报{模型数}模型评估检验报告（{成员数}成员）

**归档：{归档名}**

报告日期：{报告日期}    报告类型：集合预报（{成员数}成员）

**评估对象：{模型名列表}**

## 报告定位与衔接

报告类型：集合预报（ensemble，{成员数}成员）。{衔接说明}

本报告中的FA指Forecast Activity，统一使用全程{首时效}–{末时效}h、{时效个数}个lead，不对FA做短中长时效分段；分时效分析用于RMSE、CRPS、ACC与Spread/RMSE。

# 1. 结论摘要

{逐条结论，一条一行、以 • 开头；每条都必须带具体数值}

# 2. 样本口径与数据质量

**样本口径**

| **模型** | **请求日期** | **成功日期** | **日期目录** | **有效共同日期** | **成员数** | **Lead数** | **失败情况** |
| --- | --- | --- | --- | --- | --- | --- | --- |
{一行一个模型，按模型名字典序；最后一行是「共同有效样本」，该行的请求日期与日期目录填 —}

{数据质量逐条说明，一条一行、以 • 开头}

# 3. 评估指标与解释

**指标口径**

| **指标** | **定义/口径** | **方向** |
| --- | --- | --- |
{一行一个指标，按指标名字典序}

{指标口径补充说明}

# 4. 总体评估结果

**{模型数}模型总体指标**

| **模型（相对排序）** | **综合相对RMSE** | **CRPS** | **ACC** | **Spread偏差(绝对对数)** | **FA偏差(全程)** | **纬向谱偏差** |
| --- | --- | --- | --- | --- | --- | --- |
{一行一个模型，按综合相对RMSE升序；模型名后写「（第N）」}

## 4.1 准确度与可靠性分项结论

{逐条结论，一条一行、以 • 开头；每条都必须带具体数值}

## 4.2 逐变量RMSE

**分变量集合均值RMSE**

| **变量** | {各模型各占一列，列名即模型名} | **显著优胜** |
| --- | {每个模型补一列 ---} | --- |
{一行一个变量，按变量名的字典序}

{分变量判读句：须点名模型间差距最大与最小的变量、哪些变量上出现了模型名次反转，并说明「显著优胜」列计入的是配对检验还是仅点估计；单个变量领先不代表总体领先}

## 4.3 离散度校准

**分变量平均 Spread/RMSE 与距 1 偏差**

| **变量** | {每个模型各占一列，列名是「模型名 + 平均Spread/RMSE」} | {每个模型各占一列，列名是「模型名 + 距1」} | **更接近1** |
| --- | {每个模型补两列 ---} | --- |
{一行一个变量，按变量名的字典序}

{离散度判读说明}

## 4.4 全程FA ratio与谱结构

FA仅指Forecast Activity，全程{首时效}–{末时效}h统计，不进行时效分段，以避免与降水False Alarm混淆。

**全程FA ratio绝对偏差**

| **变量** | {每个模型各占一列，列名是「模型名 + 平均绝对ratio偏差」} | **更接近1** |
| --- | {每个模型补一列 ---} | --- |
{一行一个变量，按变量名的字典序}

**纬向谱（越大表示与观测谱的幅度结构偏差越大）**

| **谱变量** | {每个模型各占一列，列名是「模型名 + 纬向谱绝对对数偏差」} | **更优** |
| --- | {每个模型补一列 ---} | --- |
{一行一个谱变量，按变量名的字典序}

{谱判读句：须写明纬向谱偏差的排名与综合相对RMSE排名是否一致；不一致时指出方向——是幅度谱更接近但场误差更大，还是反过来，并说明这对选型意味着什么}

# 5. 分时效表现

分时效分析只覆盖RMSE、CRPS、ACC和Spread/RMSE；FA不参与分段。综合RMSE以{模型数}模型的几何均值为100。

**分时效段{模型数}模型指标**

| **时效** | **模型** | **相对综合RMSE** | **CRPS** | **ACC** | **Spread绝对对数偏差** |
| --- | --- | --- | --- | --- | --- |
{一行一个「时效段 × 模型」组合；按时效段升序，段内按模型名字典序}

## 5.1 时效结论

{逐条结论，一条一行、以 • 开头；每条都必须带具体数值}

# 6. 统计检验

{检验方法说明，须写明主检验与多重校正口径}

**分指标族显著优胜项汇总**

| **指标族** | {每个模型各占一列，列名是「模型名 + 显著优胜项」} | **无显著差异** |
| --- | {每个模型补一列 ---} | --- |
{一行一个指标族，按指标族名的字典序；无该项数据的指标族不出现在表里}

{检验结论逐条说明，一条一行、以 • 开头}

# 7. 风险与验收建议

{逐条，一条一行、以 • 开头}

# 附录：集合预报曲线图

本附录使用{模型数}个模型共同的{共同日期数}个日期（{共同日期起} 至 {共同日期止}）。以下每张图均包含{模型名列表}{模型数}条模型曲线。正文统计表与图中曲线使用同一批共同日期。

## 附 1 集合均值RMSE：{RMSE曲线变量数}个变量{模型数}模型曲线

每幅子图包含{模型名列表}{模型数}条曲线。

![ensemble_rmse_vs_lead](ensemble_rmse_vs_lead.png)

图 1：{图注}

## 附 2 Spread / Ensemble-mean RMSE：{离散度变量数}个变量{模型数}模型曲线

纵轴为集合成员标准差与集合均值RMSE之比，**理想值为1**，图中画 y=1 参考线。

![ensemble_spread_rmse_ratio_vs_lead](ensemble_spread_rmse_ratio_vs_lead.png)

图 2：{图注}

## 附 3 {CRPS变量} CRPS：{模型数}模型曲线

纵轴越低越好；{模型数}条曲线对应{模型名列表}。

![ensemble_crps_vs_lead](ensemble_crps_vs_lead.png)

图 3：{图注}

## 附 4 {ACC变量} ACC：{模型数}模型曲线

纵轴越高越好；{模型数}条曲线对应{模型名列表}。

![ensemble_acc_vs_lead](ensemble_acc_vs_lead.png)

图 4：{图注}

## 附 5 FA（Forecast Activity）：{FA曲线变量数}个变量、全程{首时效}–{末时效}h {模型数}模型曲线

每个子图均包含{模型数}条模型曲线，并覆盖全部{时效个数}个lead（{首时效}–{末时效}h）；ratio越接近1越好。

![ensemble_fa_ratio_vs_lead](ensemble_fa_ratio_vs_lead.png)

图 5：{图注}

## 附 6 纬向功率谱：{谱变量数}个变量{模型数}模型曲线

双对数坐标；{模型数}条颜色曲线对应{模型名列表}的预测谱。观测谱为黑色虚线。

![ensemble_spectrum](ensemble_spectrum.png)

图 6：{图注}

## 附 7 变量 × 时效 RMSE 热力图

{模型数}幅子图，每个模型一幅：纵轴是{RMSE曲线变量数}个变量、横轴是全部{时效个数}个lead
（{首时效}–{末时效}h）。格子里的数**不是 RMSE 本身，而是该模型在该变量、该 lead 上
相对自己首时效（{首时效}h）的 RMSE 倍数**——除以首时效就把变量的量纲与气候态差异
约掉了，{RMSE曲线变量数}个变量才能摆在同一根色标下横向比。这里的 RMSE 是**集合平均场**
口径，与正文第 4.2 节同源，不要和「附 2 的 Spread/RMSE」混读。
读法：同一行里颜色随列单调加深是正常的，**加深得慢**才说明这个变量扛得住长时效；
同一行里某一段突然跳深，通常不是模式变差，而是该时效上成员数或变量路由变了，
要回第 2 节核对样本。

![ensemble_rmse_heatmap](ensemble_rmse_heatmap.png)

图 7：{RMSE曲线变量数}个变量 × {时效个数}个 lead 的 RMSE 倍数热力图（各自除以本模型首时效 {首时效}h 的 RMSE；{共同日期起} 至 {共同日期止} 共同日期平均）

## 附 8 {谱变量} 谱比随时效

横轴为波数（双对数），纵轴为 pred/obs，y=1 参考线画出；**每个 lead 一条曲线**，
颜色由浅到深对应 lead 由短到长。这一节回答的是「小尺度能量不足是随时间恶化，
还是一开始就缺」：曲线整体贴着 1、随 lead 一起下移，是误差累积；曲线从最短时效
就整体偏低、后续几乎不再下移，是模式本身的能量谱问题，调时效救不回来。
附 6 是{共同日期数}个日期的平均谱，看不出这条随时间的变化，两张图要对着看。

![ensemble_spectrum_ratio_vs_lead](ensemble_spectrum_ratio_vs_lead.png)

图 8：{谱变量} pred/obs 谱比随预报时效的变化（{共同日期起} 至 {共同日期止} 共同日期平均）

## 附 9 单起报 {谱变量} 功率谱曲线

附 6 是{共同日期数}个日期平均后的谱，平均会把个例差异抹平。这一节换成**单个起报**
（{起报时刻}）的谱，用来核对平均谱上的结论在个例上是否成立——平均谱上「小尺度偏低」
如果只在少数个例出现，就不该写成模式的普遍特征。集合平均谱本身的平滑也会掩盖
成员间的谱差异，个例图里谱线更毛糙是正常的。

![spectrum_curve](spectrum_curve_{谱变量}_{起报}.png)

图 9：{起报时刻} 单起报的 {谱变量} 纬向功率谱（双对数；黑色虚线为同时刻观测谱）

## 附 L 分纬度带结果（可选）

本块把长表里 `region` 非空的行**单独汇总**，全球行**不参与**——全球平均与纬度带平均
是两个量，混在一起算会把带间差异整个抹平。带名与边界照搬评测配置
`options["regions"]`：**报告不翻译、也不写死任何带名**，配置里叫什么就显示什么。
表里的相对RMSE是**对本带基线**取的（100 = 该带内的{模型数}模型几何均值），
不是全球基线——热带和极区的绝对 RMSE 差一个量级，不除本带基线没法横向比。

**分纬度带综合相对RMSE**

| **纬度带** | {各模型各占一列，列名即模型名} | **Best** |
| --- | {每个模型补一列 ---} | --- |
{一行一个纬度带，按带名升序；带名写成 `tropics [-20, 20]` 这种「带名 + 边界」的形式，边界取自 manifest 的 `regions`，取不到就只写带名}

**分纬度带 Spread/RMSE（距 1 越近越好）**

| **纬度带** | {每个模型各占一列，列名是「模型名 + 平均Spread/RMSE」} | **更接近1** |
| --- | {每个模型补一列 ---} | --- |
{一行一个纬度带，与上表同序}

{分纬度带判读句：须写明各带内排名是否与全球口径一致、哪个模型在哪个带上发生了名次反转，低纬与中高纬的误差量级差几倍（量级差按同一变量跨带算比值、再取各变量的中位数，不能跨变量平均 RMSE）；并单独说明离散度是否随纬度带变化——「低纬误差小但过度离散、中高纬误差大却离散不足」是常见形态，两者的业务含义完全相反}

![lat_band_rmse_vs_lead](lat_band_rmse_vs_lead.png)

图 L1：{图注}

![lat_band_summary](lat_band_summary.png)

图 L2：{图注}

![lat_band_spread_ratio](lat_band_spread_ratio.png)

图 L3：{图注}

## 附 S 分季节结果（可选）

**本块本批未出。** 产物长表里目前没有季节维度，渲染器保留节位并标注「本批未出」，
不静默省略、也不留空表。

要接上这一块，得先把两条口径定下来（两条都不难，选错会让数对不上）：

- 季节按**起报时刻**还是**有效时刻**切——同一份检验里两者会差一个时效的长度；
- DJF 跨年怎么归——12 月与次年 1、2 月要不要算同一个 DJF。

定了之后这一块的形态与「附 L」完全一致：一张分季节表（一行一个季节，按 DJF、MAM、
JJA、SON 升序，各模型各占一列，末列 `Best`），加两张图——`season_summary.png`
（分季节综合相对RMSE 柱状图，`图 S1`）与 `season_rmse_vs_lead.png`（各季节
综合相对RMSE 随预报时效的变化，`图 S2`）。数据同样出自长表，**不需要重跑评测**。

`图 S1`/`图 S2` 两个号现在**留空**：这一块没出数就不出图，不指不存在的文件。
接上之后按「附 L」的写法补 `![…]` 与 `图 S*：{图注}` 即可。
````
