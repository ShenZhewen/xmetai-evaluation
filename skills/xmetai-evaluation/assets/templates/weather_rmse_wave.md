# 纬向 FFT 与球谐谱补充分析报告骨架

覆盖吃 `weather_rmse_single_<模型>` 产物（`scores.csv` + `manifest.json` +
`diagnostics/`）的那份**补充分析报告**——在单成员报告的口径之上再加
**纬向 FFT 功率谱**与**球谐带功率谱**两套谱检验。
确定性单成员那一份是 [`weather_rmse_single.md`](./weather_rmse_single.md)，集合那一份是
[`weather_rmse_ens.md`](./weather_rmse_ens.md)——三者是同一家族的三个口径：
`_single` / `_ens` 按指标族分，这一份专管**谱**。

**照这个填空就是最终报告。** `{…}` 是占位符，**其余每一个字逐字照抄，不许改**——
包括标点、加粗、表头与分隔行、图注的位置。

## 与另外两份骨架的差别

**这一份是补充报告，不重复主报告的任何结论性章节**（2026-09-22 简化过一次，
之前是 7 章 12 图，与 `weather_rmse_single.md` 大面积重合）。现在整块去掉的是
**主报告已经逐张表、逐张图出过的部分**：

| 去掉的 | 主报告里对应处 |
| --- | --- |
| 逐变量 RMSE（原 §4）与分时效段（原 §4.1） | `weather_rmse_single.md` §4 / §9 |
| 变量 × 时效 RMSE 热力图（原图 8） | 主报告 附 5（**同一张图**） |
| 单起报谱曲线（原图 9） | 主报告 附 6（**同一张图**） |
| 能力雷达图（原图 10） | 主报告 附 7（**同一张图**） |
| 异常与数据质量风险（原 §6）、验收结论与选型建议（原 §7） | 主报告 §6 / §7 / §11 |
| 附 L 分纬度带 | 主报告 附 L（同一张表） |

留下的是这一份独有的那条链：**总体结果 → 功率谱检验 → 附录**。
第 3 节的基线值（综合相对 RMSE / ACC / FA）**留着不是要再判一次优劣**，
是因为「相对」口径要有个 100 的基线才读得懂；判读句一律落在两套谱上。

要再删，**渲染器与骨架一起改**（`visualization/wave_report.py` 的 `render_wave`
与 `build_wave_report`），只改一边报告就对不上契约。

其余形状与 `weather_rmse_single.md` 一致：

- **章节用 `# 1.`…`# 4.` + 附录**；
- **表不编号**，直接跟在章节标题下；**图带 `图 N：` 图注**，编号按出现顺序连续；
- **原件是语雀导出**，带 `<font style="color:#…">` 彩色标签和语雀 CDN 图片链接。
  这里按本地 markdown 规范落地：**彩色标签去掉、只留文字**（「报告性质」那句
  仍然加粗），图片换成 `![名字](名字.png)`——**正文一个字没动**；
- **表格里的 `|pred/obs−1|` 会截断 markdown 表格**，这里写成 `\|pred/obs−1\|`。
  渲染出来还是 `|pred/obs−1|`，只是不再破坏表格结构；
- **中文数字由渲染器给**。原件写的是「三模型」「六个变量」「五个球谐总阶数频带」，
  不是「3 模型」「6 个变量」——`{模型数}`、`{FA变量数}` 这类占位符要填**中文数字**，
  只有 lead、波数、日期数、百分比这些才填阿拉伯数字。

## 编号契约

- 正文用 `图 N：`，编号从 `1` 起、不重号、不跳号到用不到的号。
  **`图 1`、`图 4`、`图 5`、`图 6` 必出**（每个批次都有对应数据）；
  **`图 2`、`图 3` 按球谐带的数据可得性出**——出不了时**保留节位、写明「本批未出」
  及原因**，不指一张不存在的文件（所以出现「图 2 缺、图 4 在」是正常的，
  缺的是哪一节，那节的标题下面写着）；
- 分季节可选块**预留** `图 S1：`…`图 S2：`；这一块目前本批未出，所以现在不出图、
  号留空（见文末「附 S」）。**分纬度带那块已随简化整块删除**，
  不再有 `图 L*` 这个命名空间。

**可选块**：放在报告末尾——`## 附 S 分季节结果（可选）`，位置在
`# 附录：数据与复算证据` 那张表之后，渲染器自己加的「附：图件清单」之前。
整块删掉**不需要重排任何编号**，这就是给它单开命名空间的原因；
要用就整块留着，不要只删图不删表。

附 S 的数据来自评测产物里**同一张长表**（`init_time`），**不是**另跑一次评测。
拿不到数据时**保留节位并标注「本批未出」**，不静默省略、也不留空表——
与下面 §4.1–4.3 球谐带那三节同一套约定。

## 原件没有、这一份补上的东西

原件是**语雀富文本导出**，图是裸 CDN 链接、一张图注都没有。这一份给每张图
**起了文件名、补了 `图 N：{图注}`**——否则"根据评测结果出图"时，
没有任何地方说明这张图该画成什么样。**这是唯一一处对原件的增补**，
不想要就把 `图 N：…` 那几行删掉，其余照旧。

> **本骨架目前没有测试守着**（`tests/` 在这个仓库里是空的，见 `README.md`
> 的「怎么被保证」）。按同一套格式写、能被 `assert_matches_skeleton` 直接认，
> 但**没有**对应的 `test_weather_rmse_wave.py`；将来写渲染器时得自己回来
> 对一遍骨架，别指望 CI 红给你看。
> **占位符不要跨行写**——`{` 和 `}` 两边都算占位符标记。
> **`{报告日期}` 填报告的生成日期**（`YYYYMMDD` 八位，`det_report.report_date()`
> 就是它），**不是数据末日期**——数据覆盖到哪天，正文第 2 节已经写了
> 「覆盖 N 天（起，止）」，抬头这一格只承载「这份报告是什么时候出的」。
> 代价是产物**不再逐位可复现**：同一天跑两次一样，隔天跑就不同。

> **球谐带那三节（4.1–4.3）的数据来自长表里 `spherical_bands` 展开的三行**
> （`spherical_band_power_forecast` / `_observation` / `_ratio`），
> 靠 **`group` 列**区分频带（`1_4` / `5_20` …）。
> **两种情况会让这三节出不来**：配置的 `metrics` 里压根没有 `spherical_bands`；
> 或者算了、但产物**缺 `group` 列**（长表契约 24 列，修复前的产物只有 23 列，
> 5 个频带的行混在一起认不出来）。后者**重跑评测即可**。
> 两种情形渲染器都保留节位并标注「本批未出」，不静默省略、也别留空表。

---

<!-- skeleton: A -->
````markdown
# 确定性预报{模型数}模型波谱评估报告

数据产物：{归档名}

报告日期：{报告日期}    模型：{模型名列表}    评估层级：{评估层级}

**报告性质：补充分析。本报告不修订、不替代原输出级评估检验报告，仅补充{模型数}个 single 输出的统一重算、纬向 FFT 和球谐带功率谱结果。**

# 1. 结论摘要

本报告评估{模型名列表}共{模型数}个模型，覆盖{共同日期数}天（{共同日期起} 至 {共同日期止}）；口径为纬向 FFT 与球谐带功率谱，RMSE / ACC / FA 只作基线。本报告是补充分析，不修订、也不替代原输出级评估检验报告。

{结论，一整段、不分点；把每条判断连成通顺的连续文字，依次覆盖归档完整性、纬向 FFT 谱、球谐带功率谱、选型取向；每条都必须带具体数值。**不写**精度类（综合相对 RMSE / ACC / FA）的优劣判断——那些在主报告里，这里列出来只是给第 3 节的「相对」口径当基线}

# 2. 数据范围与检验口径

| **模型** | **日期数** | **日期范围** | **共同日期** | **核心文件缺失** | **RMSE NaN** |
| --- | --- | --- | --- | --- | --- |
{一行一个模型，按模型名字典序}

所有统计均使用{模型数}个模型的共同{共同日期数}天和{时效个数}个 lead（{首时效}–{末时效}h）。RMSE 使用{RMSE变量数}个共同变量；FA 使用所有模型都提供的{FA变量列表}{FA变量数}个变量；功率谱使用{谱变量数}个变量：{谱变量列表}。{缺席模型说明句}

## 2.1 指标定义

| **指标** | **计算方式** | **方向** |
| --- | --- | --- |
| 综合相对 RMSE | 每个日期、每个变量先除以{模型数}模型几何均值，再对{RMSE变量数}个变量取几何平均；100 为{模型数}模型基线。 | 越低越好 |
| ACC | {ACC变量} 每个日期{时效个数}个 lead 的平均 ACC，再对所有日期取平均。 | 越高越好 |
| FA 偏差 | {FA变量数}个变量、全部 lead 的 \|pred/obs−1\|，再按日期和要素平均，单位 pp。 | 越低越好 |
| 纬向谱 log-RMS×100 | 对{谱变量数}个变量的纬向 FFT 谱计算 RMS[ln(pred/obs)]×100；报告同时给出全波数 1–{全波数上限} 和匹配尺度 1–{匹配尺度上限}。 | 越低越好 |
| 球谐带 log-RMS×100 | {球谐频带数}个球谐总阶数频带上计算 RMS[ln(pred/obs)]×100，是所有日期、变量、lead 的总均方根。 | 越低越好；球谐带补充谱指标 |
| 球谐带比偏差 | 所有频带、变量、lead 与日期上的 mean \|pred/obs−1\|，单位 %。 | 越低越好 |

配对比较采用{共同日期数}个日度样本的 Wilcoxon 符号秩检验，并对同一指标族的{模型对数}个模型对做 Holm 多重校正。统计显著不代表业务重要性。

# 3. 总体结果

| **模型** | **综合相对RMSE** | **ACC (%)** | **FA偏差 (pp)** | **纬向谱 RMS×100** | **球谐谱 RMS×100** | **球谐比偏差 (%)** |
| --- | --- | --- | --- | --- | --- | --- |
{一行一个模型，按综合相对 RMSE 升序}

相关图：

![overall_summary](overall_summary.png)

图 1：{图注}

# 4. 功率谱检验

归档保留两套功率谱定义：纬向 FFT 谱（`diagnostics/spectrum_by_init.csv`）和球谐带功率（`scores.csv` 里 `spherical_bands` 的结果行）。两者都只使用功率或功率谱，不包含相位；本报告分别给出完整结果，不用一套指标替代另一套。

## 4.1 球谐带分频结果

| **频带** | {每个模型各占两列，依次是「模型名 logRMS」「模型名均值比」} | **最佳** |
| --- | {每个模型补两列 ---} | --- |
{一行一个球谐频带，按频带序号升序}

{球谐带分频判读句}

![spherical_bands_logrms](spherical_bands_logrms.png)

图 2：{图注}

![spherical_bands_ratio](spherical_bands_ratio.png)

图 3：{图注}

## 4.2 球谐带分变量结果

| **变量** | {各模型各占一列，列名即模型名} | **最佳** |
| --- | {每个模型补一列 ---} | --- |
{一行一个谱变量，按变量名的字典序}

{球谐带分变量判读句，须写明逐变量口径与按频带口径的排序是否一致}

## 4.3 球谐带配对检验

| **指标** | **模型对** | **A相对B** | **Holm p** | **显著** | **胜者** |
| --- | --- | --- | --- | --- | --- |
{一行一个「指标 × 模型对」组合；先按指标出现顺序、再按模型对字典序}

{球谐带配对判读句}

## 4.4 纬向 FFT 功率谱（完整）

纬向 FFT 对每个纬圈做一维 rFFT，经度方向波数 k=1..{全波数上限}，k=0 的纬向平均已置零。纬向谱与球谐谱不是同一种变换：纬向谱保留完整 1–{全波数上限} 波数，球谐带只汇总到总阶数 {匹配尺度上限}。为避免把超高频近零观测功率的影响与可训练频带混为一谈，本节同时给出全波数和 1–{匹配尺度上限} 匹配尺度结果。

| **口径** | {各模型各占一列，列名即模型名} | **最佳** | **解释** |
| --- | {每个模型补一列 ---} | --- | --- |
| 全波数 1–{全波数上限} | {各模型的纬向谱 log-RMS×100} | {最佳模型} | 包含极高波数；受近零观测功率与数值端点影响较大 |
| 匹配尺度 1–{匹配尺度上限} | {各模型的纬向谱 log-RMS×100} | {最佳模型} | 与球谐损失的最高阶数同范围，适合与球谐带对照 |

## 4.5 五个波数带结果

| **波数带** | {每个模型各占两列，依次是「模型名 logRMS」「模型名能量比」} | **最佳** |
| --- | {每个模型补两列 ---} | --- |
{一行一个波数带，按波数带序号升序}

{波数带判读句}

![zonal_spectrum_bands](zonal_spectrum_bands.png)

图 4：{图注}

## 4.6 平均功率谱与比值曲线

下列曲线是{共同日期数}个日度功率谱的平均。黑线为 ERA5，彩色线为{模型数}个模型的预测谱；比值为 pred/obs，理想值为 1，图中断面显示范围限定为 0.1–10。

![mean_power_spectrum](mean_power_spectrum.png)

图 5：{图注}

![spectrum_ratio](spectrum_ratio.png)

图 6：{图注}

## 4.7 1–128 波数逐变量结果

| **变量** | {各模型各占一列，列名即模型名} | **最佳** |
| --- | {每个模型补一列 ---} | --- |
{一行一个谱变量，按变量名的字典序}

{波数范围敏感性判读句，须写明全波数与 1–128 两种口径下的排序是否一致、以及为什么}

# 附录：数据与复算证据

| **项目** | **值** |
| --- | --- |
| 产物目录 | {归档名} |
| 产物大小 | {归档大小} |
| SHA-256 | {SHA-256} |
| 模型 | {模型名列表} |
| 共同日期 | {共同日期数} 天，{共同日期起} 至 {共同日期止} |
| lead | {时效个数} 个，{首时效}–{末时效}h，间隔 {时效间隔}h |
| 分析结果目录 | {分析结果目录} |

## 附 S 分季节结果（可选）

**本块本批未出。** 产物长表里没有季节维度（只有 `init_time`），季节要现推；
分季节对比是**按需补**的——用户点名要哪一季、哪个纬度带，就临时切一份出来，
**不需要重跑评测**。渲染器不预生成这一块，也不静默省略、不留空表。

两条口径 2026-09-22 已定，照做即可（完整工序见
`references/rmse-batch-evaluation.md` §6「按需子集对比」）：

- 季节按**起报时刻**（`init_time`）切，**不按** `valid_time`——报告整套契约建立在
  「共同日期 = 共同 `init_date`」上，一组 `init_date` 必须干净地属于一个季节；
- DJF 按**气象冬季**归组（当年 12 月 + 次年 1、2 月）。不满整年的批次两个冬季
  **各只有一截**（实测 `20250102–20251215`：DJF(2024/25) 只有 1/2–2/28，
  DJF(2025/26) 只有 12/1–12/15），所以报 DJF 要说清是**哪一个冬季**，或者干脆
  只出 MAM/JJA/SON；**不要**把同一自然年的 12 月拼进去充数。

季节对这份补充报告尤其要紧：谱比随季节变化明显（冬夏的行星波活动差一个量级），
把四个季节平均成一条谱曲线，会把「某个模式只在一个季节小尺度偏低」这类问题
整个抹掉。补数之后这一块的形态与主报告的「附 L」一致：一张分季节表（一行一个季节，
按 DJF、MAM、JJA、SON 升序，各模型各占一列，末列 `最佳`；基线在**子集内**重算
并写在表下），加一条带天数的判读句。`season_summary.png`（`图 S1`）与
`season_rmse_vs_lead.png`（各季节综合相对RMSE 随预报时效的变化，`图 S2`）
是可选图——**没出图就别留号**，不指不存在的文件。

**本补充报告不修订、不替代原评估报告；全部结论仅来自用户指定产物目录中的{模型数}个 single 输出{补充声明句}。**
````
